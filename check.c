#include <stdio.h>
#include <math.h>//when commented, the code still works
int main(){
    float answer = floor(256)/3;//the value up takes the floor function
    printf("%.3f",answer);
    return 0;
}